#include "utilities/main.h"

/*
  there is a file in ./txt_new   that is the original file,
  file with the name "OfuscatedFile.txt", is going to be ofuscated..
  so i recommend to write the orginal file in ./txt_new and copy past it in
  the main branch.
*/

int main()
{
    processTxt(TXTNAME, AUXILIARTXT);
    return 0;
}
